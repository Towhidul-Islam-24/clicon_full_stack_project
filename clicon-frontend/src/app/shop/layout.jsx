import React from 'react'

export const metadata = {
  title: "Shop",
  description: "Clicon ecommerce shop page",
};


const layout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default layout