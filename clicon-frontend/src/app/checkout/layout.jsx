import React from 'react'

export const metadata = {
  title: "Checkout",
  description: "Clicon ecommerce checkout page",
};


const layout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default layout