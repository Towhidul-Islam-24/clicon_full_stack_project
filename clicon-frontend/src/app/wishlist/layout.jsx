import React from 'react'


export const metadata = {
  title: "Wishlist",
  description: "Clicon ecommerce Wishlist page",
};


const layout = ({children}) => {
  return (
    <div>{children}</div>
  )
}

export default layout